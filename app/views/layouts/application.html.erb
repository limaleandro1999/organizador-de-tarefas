<!DOCTYPE html>
<html>
  <head>
    <title>Organizador De Tarefas</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link rel="stylesheet" href="http://railsgirls.com/assets/bootstrap.css">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lodash.js/3.2.0/lodash.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

    <script>
      $(document).ready(function(){
        $("#em_atraso").hide();
        $("#finalizadas").hide();
        $("#em_andamento-tab").click(function(){
          $("#em_andamento").show();
          $("#em_atraso").hide();
          $("#finalizadas").hide();
        });
        $("#em_atraso-tab").click(function(){
          $("#em_andamento").hide();
          $("#em_atraso").show();
          $("#finalizadas").hide();
        });
        $("#finalizadas-tab").click(function(){
          $("#em_andamento").hide();
          $("#em_atraso").hide();
          $("#finalizadas").show();
        });
      });
    </script>
  </head>

  <body>
    <p class="navbar-text pull-right">
      <% if user_signed_in? %>
        Logado como <strong><%= current_user.email %></strong>.
        <%= link_to 'Editar Perfil', edit_user_registration_path, :class => 'navbar-link' %> |
        <%= link_to "Sair", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
      <% end %>
    </p>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
